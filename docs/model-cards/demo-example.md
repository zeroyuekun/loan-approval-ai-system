# Model Card - XGB demo-example

> **Note.** This is an illustrative demo generated without a live database. The real model card
> is regenerated by `make model-card` (or `python manage.py generate_model_card --active`) once an
> active `ModelVersion` row is present — the metrics, confusion matrix, and training metadata below
> are representative values, not measured from a real run.

_Generated from `ModelVersion 00000000-0000-0000-0000-000000000001` on 2026-04-15_

## Model Details

- **Algorithm:** xgb
- **Version:** demo-example
- **Is active:** True
- **Traffic percentage:** 100
- **File path (not user-facing):** `ml_models/demo-example.joblib`
- **Created:** 2026-04-15

## Intended Use

**Primary intended uses.** Real-time credit risk scoring of Australian personal and home loan applications: producing a calibrated approval probability, a recommended decision, adverse-action reason codes, and SHAP-based feature contributions. Designed to support — not replace — a licensed credit officer's "not unsuitable" assessment under NCCP Ch 3.

**Primary intended users.** Licensed loan officers conducting initial eligibility and serviceability assessments; compliance reviewers auditing decisioning consistency.

**Out-of-scope uses.** Business lending. Commercial/SMSF property. Reverse mortgages. Hardship renegotiation. Any decision with direct legal effect on the applicant without human officer review. Any dataset other than Australian residents.

## Factors

**Evaluation factors.** State of residence (8 values), employment type (4 values), applicant type (single/couple), sub-population segment (first-home buyer / upgrader / refinancer / personal / business / investor), age band (where derivable from supplied data).

**Relevant factors not measured.** Gender, ethnicity, sexual orientation, marital history, disability — not collected as features, by design. Proxies for these are constrained via postcode aggregation to SA3 only and monotonic constraints on income/credit/employment.


## Metrics

| Metric | Value |
|---|---|
| AUC-ROC | 0.9100 |
| Accuracy | 0.8700 |
| Precision | 0.8300 |
| Recall | 0.7900 |
| F1 | 0.8100 |
| Brier score | 0.1200 |
| Gini | 0.8200 |
| KS | 0.6500 |
| ECE | 0.0400 |
| Optimal threshold | 0.5100 |

**Confusion matrix at optimal threshold:**
```json
{
  "tp": 1800,
  "tn": 2100,
  "fp": 150,
  "fn": 200
}
```

**Calibration method:** isotonic

## Evaluation Data

- **Test set source:** held-out split from the synthetic generator
- **Split strategy:** temporal (if `application_quarter` available) else stratified random 70/15/15

## Training Data

**Training metadata:**
```json
{
  "num_records": 50000,
  "seed": 42,
  "split": "temporal",
  "notes": "Illustrative demo-example model card generated without DB access; real cards should be regenerated via make model-card once an active ModelVersion exists."
}
```

**Training parameters:**
```json
{
  "n_estimators": 400,
  "max_depth": 6,
  "learning_rate": 0.05,
  "subsample": 0.9
}
```

## Quantitative Analyses

Subgroup fairness metrics not yet computed for this version. Subgroup AUC monitoring is on the Track C roadmap - see `docs/superpowers/specs/2026-04-15-portfolio-polish-design.md` out-of-scope items.

## Ethical Considerations

- Decisions materially affect people; we require officer review for all escalated and declined cases.
- Protected attributes are not features; proxies are constrained via SA3 aggregation and monotonic constraints.
- Synthetic training data means real-world calibration on launch is unverified; shadow-mode recommended.

## Caveats and Recommendations

- Retrain when macro conditions change materially (RBA cash rate move >100bp, unemployment >1pp shift).
- Monitor PSI on key features; retrain if PSI > 0.2 on any top-10 feature.
- This card auto-generates from the ModelVersion row - fairness gaps reflect what has been computed, not what is possible.
